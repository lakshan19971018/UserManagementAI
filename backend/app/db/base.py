# from sqlalchemy import Column,Integer
# from app.db.session import Base

# class BaseModel(Base):
#     __abstract__ = True  # This ensures it is not used directly
#     id = Column(Integer, primary_key=True, index=True)